# 航空保障智能协同平台 技术选型报告

> 版本：V1.0
> 日期：2026-07-29
> 评估标准：真实企业内部航空保障系统建设标准
> 依据：`design-document.md` V1.1
> 选型硬约束：成本低 / 好维护 / 加功能成本低 / 稳定 / 安全 / 响应快
> 阶段约束：当前个人开发阶段 → 未来企业扩展阶段

---

## 1. 项目需求分析

### 1.1 业务特征

| 维度 | 特征 |
|------|------|
| 行业 | 航空运输服务分公司,航班保障现场协同 |
| 用户规模 | 一线 2000–3000,管理 100–200,峰值并发约 500(假设 A5) |
| 数据敏感性 | 高(航班计划、机位、人员档案);混合云,敏感数据不出内网(假设 A10) |
| 实时性 | 事件产生到推送 ≤5 秒;态势看板秒级刷新 |
| 可用性 | 工作时间 6:00–24:00 可用性 ≥99.5% |
| 分析负载 | 历史数据聚合,报表 ≤3 秒,非实时大数据 |
| 硬件接入 | 本期不做,RFID/UWB/智能工牌后期接入(预留接口) |
| AI 预测 | 本期仅接口预留,不实现 |

### 1.2 技术需求要点

- **强事务**:保障任务分配、事件状态流转、人员状态机,需 ACID
- **实时推送**:WebSocket + 多通道(站内/小程序订阅/企业微信/短信)
- **状态机**:人员状态、任务状态、事件状态,逻辑复杂
- **规则引擎**:预置规则 + 阈值可配,事件驱动评估
- **定时任务**:超时检查、未知状态检查、统计聚合
- **数据分析**:4 个分析子模块 + AI 预测接口预留
- **权限隔离**:班组/个人/角色三级数据范围控制 + 审计
- **混合云部署**:内网核心 + 云端员工侧,数据授权同步

### 1.3 阶段约束

- **当前**:个人开发,需快速出 MVP,运维成本极低
- **未来**:企业接手,需稳定可维护,人才接手容易,可扩展

---

## 2. 技术路线比较

### 2.1 三方案概览

| 维度 | 方案A:Go | 方案B:Java | 方案C:Python |
|------|----------|------------|--------------|
| 后端 | Go + Gin/Fiber | Spring Boot + Spring Cloud + MyBatis | FastAPI |
| 数据库 | PostgreSQL | (通常 MySQL/PostgreSQL) | PostgreSQL |
| 缓存 | Redis | Redis | Redis |
| 消息 | RabbitMQ/Kafka | MQ | (通常 Celery/Redis) |
| 前端 | Vue3/React | Vue3 | Vue3 |
| 移动端 | 微信小程序 | 微信小程序 | 微信小程序 |

### 2.2 七维度详细比较

#### 2.2.1 开发效率

| 子项 | Go | Java | Python |
|------|-----|------|--------|
| 初期开发速度 | 中(生态简洁但需自建轮子多) | 中高(starter 生态覆盖 80% 需求) | 高(语法灵活,起步快) |
| AI 辅助开发能力 | 高(语法简单,LLM 生成准) | 高(主流语言,训练语料最多) | 高(语法简单,LLM 友好) |
| 代码复杂度 | 低(语言简单,显式) | 中(注解/配置多,但规范) | 低起步,高规模时高(动态类型) |
| 学习成本 | 低(语言小) | 中(生态大) | 低(语言易) |

**分析**:
- 航空保障系统**不是脚本工具**,涉及大量事务/权限/审计/状态机/定时任务,Go 需要自建或拼装轮子(Spring Data JPA、Spring Security、Spring Scheduling 在 Java 里开箱即用)
- Python 起步快但**类型弱、重构难**,业务复杂后维护成本上升
- Java 借助 starter 生态,业务代码量反而最少

#### 2.2.2 长期维护成本

| 子项 | Go | Java | Python |
|------|-----|------|--------|
| 代码可读性 | 高(显式简单) | 高(规范强,IDE 重构强) | 中(动态类型,大项目难追) |
| 人员接手难度 | 中(Go 人才增长中但航空业少) | 低(航空/政企 Java 人才最多) | 高(航空业 Python 业务人才极少) |
| 企业维护成本 | 中(需自建运维体系) | 低(运维工具/DBA/监控最全) | 高(性能调优/部署/多版本难) |

**分析**:
- 航空业是传统行业,IT 团队以 Java 为主流,接手成本是**硬约束**
- Go 在互联网公司多,但在航空/机场 IT 部门较少,未来招人难
- Python 做业务系统在航空业几乎没有先例

#### 2.2.3 功能扩展能力(关键)

| 未来扩展 | Go | Java | Python |
|----------|-----|------|--------|
| 事件引擎 | 需自建 | Spring 事件 + 成熟方案 | 需自建 |
| 规则引擎 | 需自建或绑 Drools Go(弱) | Drools/LiteFlow/自研,生态最强 | 仅有 python-rules 等弱方案 |
| 状态计算 | 自建状态机库(Spring StateMachine) | Spring StateMachine 成熟 | 自建 |
| 数据分析 | 需对接外部 | 可内嵌或对接外部 | 原生强(Pandas) |
| AI 接口 | 可写桩,但 ML 生态弱 | 可写桩,ML 弱 | 原生最强 |
| 硬件接口 | 强(并发/MQTT 友好) | 强(生态全) | 中(性能弱) |

**分析**:
- **规则引擎/状态机/事务**是本系统核心,Java 生态压倒性领先
- **数据分析 + AI**是 Python 强项,但本期仅预留接口,不需要现在用 Python
- **硬件接口**Go/Java 都强,Python 性能弱
- 结论:**业务系统用 Java,AI 用 Python(后期)**

#### 2.2.4 性能

| 场景 | Go | Java | Python |
|------|-----|------|--------|
| 大量人员状态变化 | 极强(协程) | 强(JIT 后接近 Go) | 弱(GIL) |
| 航班事件实时推送 | 极强 | 强(WebSocket 成熟) | 弱 |
| 高并发查询 | 极强 | 强 | 弱(需异步框架勉强) |

**分析**:
- 本规模(500 并发)**三方案都够用**,Python 也勉强能撑
- Go 性能上限最高,但**本规模不是 Go 的优势发挥场景**
- Java 经 JIT 预热后性能足够,且瓶颈通常在 DB/Redis 而非语言

#### 2.2.5 稳定性

| 子项 | Go | Java | Python |
|------|-----|------|--------|
| 企业级可靠性 | 高 | 极高(航空/金融默认) | 中 |
| 长时间运行能力 | 高(内存省) | 高(JVM 成熟) | 中(内存泄漏/版本问题多) |
| 故障恢复 | 高 | 高(生态全) | 中 |

**分析**:航空业对稳定性要求极高,Java 生产验证最充分。

#### 2.2.6 安全性

| 子项 | Go | Java | Python |
|------|-----|------|--------|
| 身份认证 | 自建或绑第三方 | Spring Security 工业级 | FastAPI 简单方案 |
| 权限控制 | 自建 | Spring Security + 方法级注解 | 自建 |
| 数据隔离 | 自建 | 行级/Service 层隔离成熟 | 自建 |
| 审计日志 | 自建 | Spring AOP 一行注解 | 自建 |

**分析**:**安全是航空业红线**。Spring Security 是工业级默认,Go/Python 都需大量自建,成本高且易出漏洞。

#### 2.2.7 部署维护

| 子项 | Go | Java | Python |
|------|-----|------|--------|
| Docker 支持 | 极好(单二进制) | 好(JVM 镜像) | 中(镜像大,依赖多) |
| Linux 部署 | 极简(单二进制) | 简(JAR) | 繁(venv/依赖) |
| 运维复杂度 | 低 | 低 | 高(多版本/依赖) |
| 服务器资源 | 极低 | 中(JVM 1–2G) | 中 |

**分析**:Go 部署最轻,但**运维复杂度 Java 也不高**,且 JVM 资源对本规模可接受。

### 2.3 综合评分(加权)

> 权重依据航空业实际:安全 20% / 维护 20% / 扩展 15% / 稳定 15% / 成本 15% / 性能 10% / 效率 5%

| 维度 | 权重 | Go | Java | Python |
|------|------|-----|------|--------|
| 开发效率 | 5% | 8 | 8 | 9 |
| 长期维护 | 20% | 7 | 9 | 5 |
| 功能扩展 | 15% | 6 | 9 | 7 |
| 性能 | 10% | 10 | 8 | 5 |
| 稳定性 | 15% | 8 | 10 | 6 |
| 安全性 | 20% | 6 | 10 | 5 |
| 部署维护 | 15% | 9 | 8 | 5 |
| **加权总分** | 100% | **7.45** | **9.00** | **5.85** |

---

## 3. 推荐方案

### 推荐技术栈(方案 D:Java 模块化单体 + Python AI 微服务预留)

| 层 | 选型 | 关键理由 |
|----|------|----------|
| **后端主语言** | Java 17(LTS) + Spring Boot 3 | 航空业默认,安全/事务/生态最强 |
| **后端架构** | **模块化单体**(Modular Monolith) | 个人开发阶段不微服务化,模块边界清晰未来可拆 |
| **ORM** | MyBatis-Plus | SQL 可控,航空业习惯,统计复杂 SQL 友好 |
| **数据库** | MySQL 8.0 | 运维人才最广,事务稳定,本规模够用 |
| **缓存/队列** | Redis 7(Streams 做轻量队列) | 一物多用,降组件数 |
| **实时通信** | WebSocket(STOMP) | 态势看板实时刷新 |
| **Web 管理端** | Vue 3 + TS + Element Plus + ECharts | 中后台最低成本 |
| **移动端** | 微信小程序(原生) | 免安装、免推送服务、对接企业微信 |
| **企业 IM** | 企业微信 API(假设 A8) | 复用现有企业通讯 |
| **认证** | Spring Security + JWT(本期) → SSO(未来) | 工业级,平滑升级 |
| **规则引擎** | 自研轻量规则执行器(预置+阈值) | 不引 Drools,降维护成本 |
| **状态机** | Spring StateMachine | 人员/任务/事件状态流转 |
| **部署** | Docker Compose + Nginx | 内网轻量,免 K8s |
| **CI/CD** | GitHub Actions(self-hosted runner) | 已有仓库,零额外服务 |
| **监控** | Actuator + Prometheus + Grafana | 免费、标准 |
| **AI 预测** | 本期 Spring Boot 桩 → 未来独立 Python(FastAPI)微服务 | 本期零成本,未来不绑死 |
| **硬件接口** | 设备状态上报 API(HTTP/MQTT 可选) | 预留 RFID/UWB/智能工牌 |

### 不推荐方案及原因

- **不推荐纯 Go**:业务系统生态弱(规则引擎/状态机/安全/事务),航空业人才少,未来企业扩展会遇瓶颈
- **不推荐纯 Python**:业务系统稳定性/性能/安全弱,航空业无先例
- **不推荐一开始微服务**:个人开发阶段超配,运维成本高,模块化单体足够
- **不推荐 Drools/重型规则引擎**:与「预置+阈值可配」不匹配,维护成本高
- **不推荐 K8s**:本规模超配,Docker Compose 足够

---

## 4. 推荐原因(对齐 6 项硬约束)

| 硬约束 | 如何满足 |
|--------|----------|
| 成本低 | 全栈开源;小程序免 App 渠道;Docker Compose 免 K8s;复用企业微信;CI 用 GitHub Actions 免费;规则引擎自研不引 Drools |
| 好维护 | Spring Boot/Vue/MySQL/Redis 是航空/政企人才池最大组合;单仓多模块;Compose 即部署文档;MyBatis SQL 可控可审计 |
| 加功能成本低 | Spring starter 生态;Element Plus 组件全;模块化单体新增模块快;规则引擎结构化可扩展;推送通道抽象可插拔 |
| 稳定 | 工业级成熟栈;JVM/MySQL/Redis 大量生产验证;Docker 隔离;Spring StateMachine 状态流转可靠 |
| 安全 | Spring Security + JWT + RBAC;HTTPS 全链路;数据范围 Service 层隔离;Spring AOP 审计日志;内网核心数据不出网 |
| 响应快 | JVM JIT;Redis 内存缓存;WebSocket 实时;汇总表优化分析;Vite 首屏快 |

---

## 5. 系统架构设计

### 5.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              用户层                                       │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐                 │
│  │ Web 管理端    │   │ 微信小程序    │   │ 企业微信      │                 │
│  │ (值班经理/    │   │ (一线人员)    │   │ (消息通道)    │                 │
│  │  班组长/管理员)│   │              │   │              │                 │
│  │ Vue3+ElemPlus │   │ 原生小程序    │   │              │                 │
│  └──────┬───────┘   └──────┬───────┘   └──────┬───────┘                 │
└─────────┼──────────────────┼──────────────────┼─────────────────────────┘
          │HTTPS             │HTTPS             │回调
          ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        云端接入区(员工侧,授权访问)                       │
│  ┌──────────────────────────────────────────────────┐                   │
│  │ Nginx 网关(HTTPS 终结 + 路由 + 限流)             │                   │
│  └────────────────────┬─────────────────────────────┘                   │
│  ┌────────────────────▼─────────────────────────────┐                   │
│  │ 移动端 BFF(可选,小程序 API 聚合)                │                   │
│  └────────────────────┬─────────────────────────────┘                   │
│  ┌────────────────────▼─────────────────────────────┐                   │
│  │ 通知/任务缓存库(Redis,员工可见摘要)              │                   │
│  └──────────────────────────────────────────────────┘                   │
└─────────────────────────────┬───────────────────────────────────────────┘
                              │ 专线/VPN(仅授权数据回传)
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     内网核心区(敏感数据 + 核心业务)                      │
│  ┌──────────────────────────────────────────────────┐                   │
│  │ Nginx(内网反向代理 + 静态资源)                   │                   │
│  └────────────────────┬─────────────────────────────┘                   │
│  ┌────────────────────▼─────────────────────────────┐                   │
│  │         Spring Boot 模块化单体(Java 17)          │                   │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐    │                   │
│  │  │ flight     │ │ task       │ │ event      │    │                   │
│  │  │ 航班模块    │ │ 任务模块    │ │ 事件模块    │    │                   │
│  │  └────────────┘ └────────────┘ └────────────┘    │                   │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐    │                   │
│  │  │ personnel  │ │ rule       │ │ analytics  │    │                   │
│  │  │ 人员模块    │ │ 规则引擎    │ │ 分析模块    │    │                   │
│  │  └────────────┘ └────────────┘ └────────────┘    │                   │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐    │                   │
│  │  │ push       │ │ device     │ │ prediction │    │                   │
│  │  │ 推送模块    │ │ 设备接口    │ │ AI 预测桩   │    │                   │
│  │  │(通道抽象)  │ │ (RFID预留) │ │ (桩)       │    │                   │
│  │  └────────────┘ └────────────┘ └────────────┘    │                   │
│  │  ┌────────────┐ ┌────────────┐                    │                   │
│  │  │ auth       │ │ common     │                    │                   │
│  │  │ 认证/权限   │ │ 审计/工具  │                    │                   │
│  │  └────────────┘ └────────────┘                    │                   │
│  └─────┬──────────────┬──────────────┬───────────────┘                   │
│        │              │              │                                   │
│  ┌─────▼─────┐  ┌─────▼─────┐  ┌─────▼─────┐                           │
│  │ MySQL 8.0 │  │ Redis 7   │  │ WebSocket │                           │
│  │ 主库+汇总表│  │ 状态/队列  │  │ (内置)    │                           │
│  └───────────┘  └───────────┘  └───────────┘                           │
└─────────────────────────────┬───────────────────────────────────────────┘
                              │ 预留 HTTP/MQTT
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     硬件接入层(未来,本期仅接口预留)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                 │
│  │ RFID 读写器  │  │ UWB 定位     │  │ 智能工牌      │                 │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘                 │
│         │                 │                 │                           │
│         ▼                 ▼                 ▼                           │
│  ┌──────────────────────────────────────────────────┐                   │
│  │ 设备接入网关(MQTT/HTTP 上报 → 设备状态 API)      │                   │
│  └──────────────────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                     AI 预测服务(未来,本期仅桩)                          │
│  ┌──────────────────────────────────────────────────┐                   │
│  │ Python FastAPI 微服务(未来)                      │                   │
│  │ 输入:历史保障/航班/人员/事件/任务数据             │                   │
│  │ 输出:风险预测/资源需求预测/事件概率预测           │                   │
│  └──────────────────────────────────────────────────┘                   │
│  本期:Spring Boot 内 Controller 返回 not_enabled                        │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.2 模块职责

| 模块 | 职责 |
|------|------|
| flight | 航班数据录入/查询/同步接口预留 |
| task | 任务模板/实例/分配/状态流转 |
| event | 事件产生/影响分析/处理/关闭 |
| personnel | 人员状态机/状态感知/设备融合预留 |
| rule | 规则定义/执行/命中历史 |
| analytics | 保障运行/事件/资源/趋势分析 + AI 接口桩 |
| push | 多通道推送(站内/小程序/企业微信/短信抽象) |
| device | 设备状态上报接口预留(RFID/UWB/工牌) |
| prediction | AI 预测接口桩 |
| auth | 认证/角色/数据范围权限 |
| common | 审计/异常/工具/配置 |

---

## 6. 数据架构设计

### 6.1 存储分层

| 数据类别 | 存储 | 说明 |
|----------|------|------|
| 业务主数据(航班/任务/事件/人员/规则) | MySQL | 强事务,内网核心区 |
| 实时状态(人员状态/会话/热点) | Redis | 内存级响应 |
| 推送队列 | Redis Streams | 削峰去重 |
| 统计汇总(分析中心) | MySQL 汇总表 | §9.3 三张表,定时聚合 |
| 员工侧通知摘要 | Redis(云端) | 仅授权访问的摘要,不含敏感明细 |
| 审计日志 | MySQL + 文件 | 关键操作落库 + 文件备份 |
| AI 预测请求(预留) | prediction_interface 表 | 本期仅 not_enabled 记录 |

### 6.2 数据流向

```
业务写入 → MySQL 主库 → 实时状态同步 Redis
                    ↓
        定时任务聚合 → 汇总表(analytics 查询)
                    ↓
        员工可见摘要 → 云端 Redis(授权访问)
                    ↓
        员工操作回传 → 内网主库
```

### 6.3 数据保留(对齐 A7)

- 航班保障过程数据 ≥1 年
- 事件数据 ≥2 年
- 审计日志 ≥1 年
- 分析汇总数据 ≥3 年

---

## 7. 部署方案

### 7.1 内网核心区(单机或双机)

```yaml
# docker-compose.yml 概念
services:
  nginx:        # 反向代理 + 静态资源 + HTTPS
  app:          # Spring Boot 模块化单体
  mysql:        # 主库(可主从)
  redis:        # 状态/队列/缓存
  prometheus:   # 指标采集
  grafana:      # 监控展示
```

- 资源建议:8C16G 单机可起步,双机主从更稳
- 部署方式:Docker Compose 一键起停
- 配置:环境变量 + application.yml,不引入配置中心

### 7.2 云端接入区

```yaml
services:
  nginx-gateway:  # HTTPS 终结 + 限流
  bff:            # 小程序 API 聚合(可选,初期可并入主应用)
  redis-cloud:    # 通知/任务摘要缓存
```

- 资源建议:4C8G 单机
- 与内网:专线/VPN,仅授权数据回传

### 7.3 CI/CD

- GitHub Actions + self-hosted runner(内网)
- 流程:推送 → 构建 → 测试 → 镜像 → 内网部署
- 小程序:微信开发者工具 CI

---

## 8. 安全方案

### 8.1 认证授权

- Spring Security + JWT(本期账号密码)
- 未来升级 OAuth2 SSO(对接 AD/LDAP),Spring Security 原生支持
- 角色 RBAC + 数据范围(班组/个人/区域)三层控制

### 8.2 数据安全

- 全链路 HTTPS(内网自签 + 云端公网证书)
- 密码 BCrypt 存储
- 敏感数据不出内网(假设 A10)
- 云端仅授权访问通知摘要,不含业务明细
- 数据范围在 Service 层强制,防止越权

### 8.3 审计

- Spring AOP 注解式审计日志
- 关键操作:用户/任务/事件/规则变更、登录、数据导出
- 审计日志独立表 + 文件双写

### 8.4 接口安全

- JWT 鉴权 + 接口权限注解
- 限流(Nginx + 应用层)
- 参数校验(Bean Validation)
- SQL 注入防护(MyBatis 参数化)
- XSS/CSRF 防护

---

## 9. 未来扩展路线

### 9.1 阶段演进

| 阶段 | 架构 | 说明 |
|------|------|------|
| 当前(个人开发) | 模块化单体 + Docker Compose | 快速出 MVP,运维成本极低 |
| 企业接手初期 | 模块化单体 + 双机主从 + 监控完善 | 稳定运行,人才接手 |
| 流量增长 | 单体拆分高负载模块(如 push/analytics) | 按需拆,不过早 |
| 多机场/分公司 | 模块化单体多实例 + 数据隔离 | 横向扩展 |
| AI 预测上线 | 新增 Python FastAPI 微服务 | 主系统只调 HTTP,不改 |
| 硬件接入 | 新增设备接入网关(MQTT) | 通过预留 device API |

### 9.2 拆分预案

模块化单体的模块边界清晰,未来可按以下顺序拆分:
1. **push 推送服务**(流量大、通道多)→ 独立服务
2. **analytics 分析服务**(重查询、可读库)→ 独立服务 + 只读副本
3. **device 设备接入网关**(协议重、IO 密集)→ 独立服务
4. **prediction AI 服务**(Python)→ 独立微服务

拆分时模块内代码已解耦,迁移成本低。

---

## 10. 风险分析

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| 个人开发阶段 Spring Boot 上手慢 | 中 | 中 | AI 辅助 + starter 生态 + 模块化清晰 |
| MySQL 分析负载增长 | 低 | 中 | 汇总表 + 索引;接口抽象可迁 PostgreSQL/ClickHouse |
| 小程序订阅消息频次限制 | 中 | 中 | 站内消息兜底;企业微信补位;模板提前申请 |
| 规则引擎自研不够灵活 | 低 | 中 | 仅预置+阈值;结构化可扩展;不追求全可视化 |
| JVM 资源占用 | 低 | 低 | 本规模可接受;Docker 资源限制 |
| 未来 Go 拥护者质疑选 Java | 中 | 低 | 本报告已论证:航空业安全/生态/人才 Java 占优 |
| 模块化单体后期难拆 | 低 | 中 | 模块边界严格 + 接口层抽象,预案清晰 |
| 企业微信 API 变更 | 低 | 中 | 推送通道抽象,可换钉钉/飞书 |
| 硬件接入协议不统一 | 中 | 中 | device API 抽象 + 网关层适配 |

---

## 特殊问题回答

### 问题1:Go 是否比 Java 更合适?未来 Go 会遇企业级扩展问题吗?

**结论:本场景 Java 更合适,Go 未来会遇企业级扩展问题。**

理由:
- **本场景不是高并发互联网应用**,是航空企业内部系统,核心难点是**事务/权限/审计/状态机/规则引擎**,这些 Java 生态(Spring Security、Spring StateMachine、JPA 事务、AOP 审计)开箱即用,Go 需大量自建
- **航空业安全红线**:Spring Security 是工业级默认,Go 安全框架碎片化,自建易出漏洞
- **人才接手**:航空/机场 IT 团队以 Java 为主,Go 人才稀缺
- **Go 未来扩展问题**:
  - 规则引擎:Go 无成熟方案,Drools Go 弱
  - 状态机:需自建
  - 事务/审计:需自建
  - 企业级监控/追踪:生态不如 JVM
- Go 的优势(高并发、低资源、单二进制)在本规模(500 并发、内网部署)**不是决定性因素**

**Go 适合的场景**:高并发 API 网关、云原生中间件、网络服务——不是本系统。

### 问题2:一开始微服务还是模块化单体?

**结论:先模块化单体(Modular Monolith),不要一开始微服务。**

理由:
- **当前个人开发阶段**:微服务超配,运维成本(服务发现/网关/链路追踪/配置中心/分布式事务)远超收益
- **本规模(500 并发)**:单体完全够用
- **模块化单体的关键**:模块边界清晰(包结构 + 接口抽象 + 数据库 schema 分组),未来可拆
- **拆分预案已设计**(§9.2),按 push/analytics/device/prediction 顺序按需拆
- **过早微服务的代价**:开发慢、调试难、部署繁、运维贵——个人开发阶段致命

**何时考虑拆分**:
- 某模块流量/资源压力明显独立(如 push 推送洪峰)
- 团队规模扩大,多团队并行开发
- 多机场多实例部署

### 问题3:后端统一语言,还是 Go 业务 + Python 数据分析/AI?

**结论:业务系统统一 Java,AI 用 Python(后期独立微服务)。**

理由:
- **业务系统统一语言**:降低技术栈复杂度,降低人才要求,降低联调成本
- **Java 做业务**:事务/安全/规则/状态机生态最强
- **Python 做 AI**:ML 生态最全,预测场景默认选型
- **本期 AI 仅预留接口**:Spring Boot 内写桩,零 Python 成本
- **未来 AI 上线**:Python FastAPI 独立微服务,主系统通过 HTTP 调用,**主系统不改语言**
- **不推荐 Go+Python 混合**:Go 业务生态弱,混合栈增加复杂度,无额外收益

**语言分工**:
- Java:所有业务模块(flight/task/event/personnel/rule/analytics/push/device/auth)
- Python:仅未来 AI 预测微服务(prediction)

### 问题4:未来接入智能工牌/RFID/UWB,架构如何设计?

**结论:通过设备接入网关 + 设备状态上报 API,主系统不改。**

架构:

```
硬件设备(RFID/UWB/工牌)
        │ MQTT / HTTP
        ▼
┌──────────────────────────┐
│ 设备接入网关(独立部署)   │  ← 未来新增,可 Go/Java 写
│ - 协议适配(MQTT/HTTP)   │
│ - 数据清洗/去重          │
│ - 位置→区域映射          │
└────────────┬─────────────┘
             │ HTTP(标准化)
             ▼
┌──────────────────────────┐
│ device 模块(主系统内)    │  ← 本期已预留 API
│ POST /api/v1/device/status│
│ - 人员 ID/设备 ID/区域/   │
│   状态/时间戳/置信度      │
└────────────┬─────────────┘
             ▼
┌──────────────────────────┐
│ personnel 模块(主系统内) │
│ - 状态融合策略            │
│ - RFID 事件 + 任务事件    │
│ - 状态机不变,增加输入源   │
└──────────────────────────┘
```

设计要点:
- **主系统不改**:device 模块本期预留 API,后期只需实现接收逻辑
- **网关独立**:协议适配/清洗在网关层,不污染主系统
- **状态融合在 personnel**:状态机本身不变,RFID 作为第二输入源
- **网关语言可选**:Go(并发强)/Java(统一栈)均可,通过 HTTP 解耦
- **渐进接入**:可先接 RFID(简单),再接 UWB(高精度),网关层适配

---

## 总结

| 决策点 | 结论 |
|--------|------|
| 后端语言 | **Java 17 + Spring Boot 3** |
| 架构形态 | **模块化单体**(非微服务) |
| 数据分析/AI 语言 | **Python(未来独立微服务)**,本期 Java 桩 |
| 数据库 | **MySQL 8.0** |
| 缓存/队列 | **Redis 7** |
| 部署 | **Docker Compose + Nginx** |
| 硬件接入 | **设备接入网关 + 预留 device API** |
| 拆分时机 | **按需拆,非一开始** |

**核心理念**:
> 航空保障系统是**企业级内部系统**,不是互联网高并发应用。选型应以**安全/稳定/可维护/人才接手**为第一优先,而非语言性能上限。Java 在航空业生态最成熟,模块化单体在个人开发阶段成本最低,Python 留给未来 AI。**不超配,不过早优化,预留清楚。**

---

*本报告为技术选型最终结论,将同步更新 `tech-stack.md`,并作为 `implementation-plan.md` 的输入依据。*
