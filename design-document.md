# 航空保障智能协同平台 设计文档

> 版本：V1.1
> 日期：2026-07-29
> 状态：草案（待评审）
> 依据：`航空保障智能协同平台_PRD.md` V1.0
> 变更：V1.1 明确混合云数据分布、RFID 硬件接入路径、新增「运行数据分析中心」模块、AI 预测接口预留

---

## 0. 关键假设与待确认事项

> 本节列出在需求澄清过程中未明确、但设计必须先做假设的决策点。每一项假设均需在评审阶段由产品/业务方确认或修正，修正后需同步更新本文档相关章节。

| 编号 | 假设项 | 当前假设 | 影响章节 | 备注 |
|------|--------|----------|----------|------|
| A1 | 人员状态感知落地方式 | 本期仅做软件层状态机（任务事件驱动），**RFID 等硬件感知设备后期接入**，作为状态机的第二输入源与任务事件融合，不改变状态机本身 | §5.1、§6.1 | 已与业务方确认：后期部署 RFID 等硬件记录员工「保障中/保障完成」状态 |
| A2 | 事件触发方式 | 系统自动产生为主，允许人工上报异常事件 | §6.2 | 已确认 |
| A3 | 规则引擎可配置程度 | 核心规则预置，阈值类参数（需求量、超时、提前预警时间）可由管理员配置 | §6.4 | 已确认 |
| A4 | 推送通道 | 站内消息 + App 推送 + 企业 IM（至少接入一种）三通道；短信作为紧急事件保底（可选） | §6.3 | 已确认满足 |
| A5 | 用户规模 | 单机场/分公司级别：一线保障人员约 **2000–3000**（已扩容），管理岗位约 100–200，峰值并发约 500 | §10 非功能性需求 | 已确认需扩容；具体数值待业务方最终核定 |
| A6 | 认证方式 | 企业账号体系，支持对接 AD/LDAP 或 OAuth2 SSO；本期至少实现账号密码 + 角色权限 | §4.2 | |
| A7 | 数据保留 | 航班保障过程数据保留 ≥ 1 年，用于复盘分析；事件数据保留 ≥ 2 年；分析中心聚合数据 ≥ 3 年 | §10.4 | |
| A8 | 企业 IM 选型 | 本期优先接入企业微信；钉钉/飞书预留接口 | §6.3 | 可调整 |
| A9 | 航班数据人工录入范围 | 录入航班计划（航班号、机型、起降时间、机位、登机口）与关键动态（延误、登机口变更、取消） | §6.5 | 后续对接 FOC/DCAC 时替换 |
| A10 | 混合云数据分布 | **航班等敏感数据在内网核心区**；**通知工作、保障任务等面向员工的通知信息在云端**，但云端数据**需授权才能访问** | §4.1 | 已确认：敏感数据不出内网，员工侧通知信息在云端 |
| A11 | 运行数据分析中心范围 | 本期建设保障运行分析/事件分析/资源分析/趋势分析 4 个子模块；**AI 预测仅预留接口层与数据结构，不实现任何预测功能** | §8.10、§9.3、§14 | 已确认：当前不实现 AI/ML/预测，仅接口与架构预留 |

---

## 1. 概述

### 1.1 产品定位

面向航空运输服务分公司的**航班保障智能协同平台**。通过保障任务模型、岗位模型、人员状态感知、事件模型与规则引擎，实现保障信息精准传递、现场状态可感知、风险提前发现、多岗位协同提升。

本产品**不替代**现有保障系统，而是建立航空保障现场的**数字化协同能力**。

### 1.2 核心目标

> 让正确的信息，在正确时间，传递给正确的人。

- 航班保障数字化管理
- 保障人员状态感知
- 事件自动识别
- 精准协同
- 运行复盘分析

### 1.3 建设范围

依据 PRD 第 15 节的三阶段规划，**本期建设范围 = 阶段一 + 阶段二**：

- **阶段一（保障协同平台）**：任务模型、事件模型、规则引擎、精准推送、运行态势、岗位工作台
- **阶段二（人员状态感知）**：以软件层状态机先行落地，RFID 等硬件感知设备后期接入（预留设备状态上报接口）
- **运行数据分析中心**（本期新增）：保障运行分析、事件分析、资源分析、趋势分析；**AI 预测仅预留接口与数据结构，不实现预测功能**（假设 A11）
- **阶段三（智能风险预测与辅助调度）**：**本期不含**，仅在运行数据分析中心与系统架构层预留 AI 预测能力，方便未来扩展

---

## 2. 范围与非目标

### 2.1 本期范围（In Scope）

1. 岗位模型与能力标签管理
2. 航班保障任务库与任务流转
3. 人员状态模型（软件层状态机，RFID 硬件接入预留）
4. 保障能力计算与可视化
5. 事件模型与事件全生命周期管理
6. 精准信息推送（站内 + App + 企业 IM，短信可选）
7. 规则引擎（预置规则 + 阈值可配置）
8. 运行态势中心、航班详情、事件中心、岗位工作台、人员状态、任务库
9. **运行数据分析中心**（保障运行分析、事件分析、资源分析、趋势分析 4 个子模块）
10. **AI 预测接口层与数据结构预留**（不实现预测功能）
11. 航班数据人工录入与维护
12. 用户、角色、权限管理

### 2.2 非目标（Out of Scope）

- 自动配载计算
- 自动安全放行
- 替代运行控制系统
- 替代人工决策
- **AI 预测、机器学习模型、自动预测功能**（阶段三，本期仅接口与架构预留）
- **RFID 等硬件感知设备部署**（本期仅预留设备状态上报接口，后期接入）
- 航班运行系统对接（本期人工录入，接口层预留）
- 运行数据分析中心**不替代**事件中心与运行态势中心（非实时监控）

---

## 3. 角色与权限

### 3.1 角色定义

| 角色 | 职责 | 主要终端 |
|------|------|----------|
| 值班经理 | 查看整体运行态势、风险航班、保障能力，处置重大事件 | Web（PC） |
| 班组长 | 管理岗位资源、处理保障异常、闭环保障任务 | Web（PC）+ 移动端 |
| 一线保障人员 | 接收并执行保障任务、上报异常、状态自动更新 | 移动端（App/小程序） |
| 系统管理员 | 管理岗位/任务库/规则/用户/权限 | Web（PC） |

### 3.2 权限矩阵（概要）

| 功能模块 | 值班经理 | 班组长 | 一线人员 | 系统管理员 |
|----------|----------|--------|----------|------------|
| 运行态势中心 | 查看 | 查看本班组范围 | 仅看本人相关 | 查看 |
| 航班详情 | 查看全部 | 查看负责航班 | 查看本人任务航班 | 查看 |
| 事件中心 | 查看+处置 | 查看+处置本班组 | 查看+上报 | 配置 |
| 岗位工作台 | 查看 | 管理 | 执行 | 查看 |
| 人员状态 | 查看全部 | 查看本班组 | 查看本人 | 查看 |
| 任务库 | 查看 | 查看 | 查看 | 管理 |
| 规则引擎 | 查看 | 查看 | - | 管理 |
| **运行数据分析中心** | 查看运行相关数据 | 查看负责区域和岗位数据 | **不开放** | 查看全部数据 |
| 系统管理 | - | - | - | 全部 |

> 数据范围控制：班组长仅可见本班组人员与负责航班；一线人员仅可见本人任务与相关信息；运行数据分析中心对一线人员不开放。

---

## 4. 系统上下文

### 4.1 部署形态（混合云，假设 A10）

**数据分布原则**：
- **内网核心区**：航班等敏感数据、保障过程数据、事件数据、人员档案、统计分析原始数据
- **云端接入区**：面向员工的通知信息、保障任务派发信息、推送内容；**云端数据需授权才能访问**
- **内网 → 云端**：仅同步「员工可见的通知/任务」摘要，不同步敏感业务明细
- **云端 → 内网**：员工操作回传（任务接收/完成、事件上报）经网关写回内网

```
┌─────────────────────────────────────────────────────────────────────┐
│  内网核心区（机场/分公司内网）—— 敏感数据与核心业务                    │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │ Web 应用（管理端）│  │ API 服务集群     │  │ 分析服务         │  │
│  │ 值班经理/班组长   │  │ (业务/规则/事件) │  │ (统计分析)       │  │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │ 主数据库          │  │ 缓存/消息队列     │  │ 分析数据仓库     │  │
│  │ (航班/保障/事件)  │  │ (实时状态/推送)  │  │ (统计/聚合)      │  │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘  │
│  ⚑ 敏感数据：航班计划、机型、机位、保障明细、人员档案、事件全量数据   │
└─────────────────────────────────────────────────────────────────────┘
              ▲                                  ▲
              │内网访问                           │专线/VPN（仅授权数据同步）
              │                                  │
┌─────────────┴──────────────┐   ┌───────────────┴──────────────────┐
│  云端接入区（员工侧）        │   │  外部集成区                       │
│  ┌──────────────────┐      │   │  ┌──────────────────┐             │
│  │ 移动端网关        │      │   │  │ 企业 IM 推送网关  │             │
│  │ (App/小程序 API)  │      │   │  │ (企微/钉钉/飞书)  │             │
│  └──────────────────┘      │   │  └──────────────────┘             │
│  ┌──────────────────┐      │   │  ┌──────────────────┐             │
│  │ 通知/任务缓存库   │      │   │  │ 短信网关(可选)    │             │
│  │ (员工可见的摘要)  │      │   │  └──────────────────┘             │
│  └──────────────────┘      │   └──────────────────────────────────┘
│  ┌──────────────────┐      │
│  │ App 推送服务      │      │
│  │ (APNs/厂商通道)   │      │
│  └──────────────────┘      │
│  ⚑ 云端数据：仅通知/任务摘要，需授权访问，不含敏感业务明细             │
└────────────────────────────┘
```

- **核心业务与敏感数据**：部署在内网，数据不出内网
- **移动端接入**：通过云端网关，一线人员经互联网访问云端缓存的通知/任务摘要
- **授权控制**：云端数据访问需鉴权；员工仅可见与自己相关的通知/任务
- **回传链路**：员工在移动端的操作（接收/完成任务、上报事件）经网关写回内网主库
- **分析数据**：分析中心在内网基于主库数据计算，结果供管理端查看

### 4.2 认证与账号

- 企业账号体系，支持对接 AD/LDAP 或 OAuth2 SSO（假设 A6）
- 本期至少实现：账号密码登录 + 角色权限控制
- 移动端支持令牌（Token）登录，与 Web 端账号体系统一
- 会话超时：Web 端 30 分钟，移动端 7 天（可配置）

---

## 5. 核心业务模型

### 5.1 人员状态模型

**状态定义**

| 状态 | 含义 | 进入条件 |
|------|------|----------|
| 空闲 | 可接受保障任务 | 登录系统且无进行中任务，或上一任务完成 |
| 保障中 | 正在执行保障任务 | 领取/被分配任务并确认 |
| 已完成 | 刚完成一项任务 | 任务状态置为完成（短暂态，随即回空闲） |
| 不可用 | 暂时无法执行任务 | 请假/离岗标记、设备故障 |
| 未知 | 状态无法判断 | 长时间无任何事件（超阈值） |

**状态流转（软件层状态机，假设 A1）**

```
        ┌──────────┐
        │   空闲   │◄─────────────────┐
        └────┬─────┘                  │
             │ 领取/分配任务            │ 任务完成
             ▼                        │
        ┌──────────┐                  │
        │  保障中  │──────────────────►│
        └────┬─────┘   (任务完成)      │
             │                        │
             │ 请假/离岗                │
             ▼                        │
        ┌──────────┐   销假/返岗  ┌────┴─────┐
        │  不可用  │────────────►│  已完成   │
        └──────────┘             └──────────┘
             ▲                        │
             │ 长时间无事件             │ (短暂态)
             │                        │
        ┌────┴─────┐                  │
        │   未知   │◄─────────────────┘
        └──────────┘
```

**硬件感知接入（RFID，后期接入）**：后期部署 RFID 等硬件设备后，设备主动上报员工状态（如「保障中」「保障完成」），作为状态机的**第二输入源**，与任务事件融合。本期接口预留但不实现设备端。

**双源融合策略（后期实现）**：
- 软件事件（任务领取/完成）与 RFID 设备事件（进入/离开保障区域）按优先级融合
- RFID 事件作为状态校验与实时性补充，减少对员工手动操作的依赖
- 状态机本身不变，仅增加输入源

### 5.2 岗位模型

**结构**：岗位类别 → 岗位 → 能力标签

```
岗位类别: 登机口保障
  └─ 岗位: 登机口保障员
       ├─ 能力标签: 登机组织
       ├─ 能力标签: 旅客引导
       └─ 能力标签: 异常协调

岗位类别: 值机保障
  └─ 岗位: 值机员
       ├─ 能力标签: 常规值机
       └─ 能力标签: 特殊旅客保障
```

- 岗位与能力标签由系统管理员维护
- 一人可具备多个岗位/能力标签
- 任务库中每个任务定义「需求岗位 + 需求能力 + 需求人数」

### 5.3 航班保障任务库

按航班生命周期分阶段，每阶段含若干任务模板：

| 阶段 | 任务模板 |
|------|----------|
| 航前准备 | 航班保障启动、资源准备 |
| 旅客服务 | 值机保障、特殊旅客保障 |
| 登机准备 | 登机口准备、广播准备、引导准备 |
| 登机 | 登机组织、异常处理 |
| 离港 | 保障结束、异常关闭 |

**任务属性**：
- 任务模板 ID、名称、所属阶段、需求岗位、需求能力、需求人数
- 计划开始/结束时间（相对航班节点的偏移）
- 超时阈值、提前预警时间（可配置，假设 A3）
- 关联航班节点（如登机开始、舱门关闭）

**任务实例**：每个航班按任务库自动生成任务实例，分配给具备能力的人员执行。

### 5.4 保障能力模型

**定义**：对单个航班，判断某类保障资源是否满足需求。

```
航班 CA1234 / 登机保障
  需求: 2 人 (登机组织能力)
  当前: 2 人保障中
  结果: 保障能力正常 ✅

航班 CA5678 / 登机保障
  需求: 2 人
  当前: 1 人保障中, 1 人空闲且具备能力
  结果: 保障能力预警 ⚠️ (仅 1 人在岗)

航班 CA9012 / 登机保障
  需求: 2 人
  当前: 0 人, 空闲具备能力 0 人
  结果: 保障能力不足 ❌
```

**能力等级**：正常 / 预警 / 不足，分别用绿/黄/红展示。

---

## 6. 关键机制设计

### 6.1 人员状态感知（软件先行 + RFID 后期接入，假设 A1）

**本期实现**（软件层）：
- 状态由任务事件驱动（领取/完成/上报）
- 人员登录/登出、任务状态变化触发状态更新
- 长时间无事件自动转「未知」并预警

**RFID 硬件接入预留**：
- 设备状态上报 API：`POST /api/v1/device/status`
- 上报内容：人员 ID、设备 ID（RFID 标签）、位置区域、状态（保障中/保障完成）、时间戳、置信度
- 后期 RFID 部署后，设备上报「进入保障区域=保障中」「离开保障区域=保障完成」等状态
- 状态融合策略：RFID 事件与任务事件按优先级融合（后期实现），状态机本身不变

### 6.2 事件模型

**事件生命周期**：

```
事件产生 → 影响分析 → 精准通知 → 处理执行 → 关闭复盘
```

**事件类型与触发方式**（假设 A2）：

| 事件类型 | 自动触发 | 人工上报 |
|----------|----------|----------|
| 航班变化事件 | 航班延误/取消/登机口变更 | - |
| 人员资源事件 | 人员不足、状态异常（未知/不可用增多） | - |
| 任务执行事件 | 任务超时、任务失败 | - |
| 旅客保障事件 | 特殊旅客任务到达 | 旅客突发情况 |
| 设备资源事件 | - | 设备故障、设施异常 |

**事件属性**：
- 事件 ID、类型、级别（信息/预警/告警/紧急）
- 来源（自动/人工）、触发时间、关联航班/任务/人员
- 影响范围（岗位、人员清单）
- 处理状态（待处理/处理中/已关闭/已复盘）
- 处理记录、关闭时间、复盘结论

### 6.3 精准信息推送

**推送原则**：不向无关人员发送无关信息。

**推送流程**：
```
事件发生 → 判断影响岗位 → 匹配保障人员 → 选择通道 → 精准推送
```

**通道与策略**（假设 A4、A8）：

| 通道 | 用途 | 触发条件 |
|------|------|----------|
| 站内消息（App/Web） | 常规任务、状态更新 | 所有事件 |
| App 推送（APNs/厂商） | 重要事件，一线离线也需触达 | 预警/告警/紧急级 |
| 企业 IM（企微优先） | 重要事件，复用企业通讯 | 预警/告警/紧急级 |
| 短信（可选保底） | 紧急事件最后兜底 | 紧急级且其他通道未送达 |

**去重与合并**：同一事件短时间内对同一用户只推送一次；多个相关事件可合并为一条摘要。

**送达确认**：站内消息需记录已读状态；App 推送与企业 IM 记录送达回执。

### 6.4 规则引擎

**规则类型**（PRD 第 10 节，假设 A3）：

| 规则类型 | 作用 | 可配置项 |
|----------|------|----------|
| 人员资源规则 | 判断岗位人员是否满足需求 | 需求人数、预警阈值 |
| 任务进度规则 | 判断任务是否按流程推进 | 超时时间、提前预警时间 |
| 航班变化规则 | 判断影响范围 | 影响岗位映射 |
| 资源冲突规则 | 判断人员任务冲突 | 冲突判定窗口 |

**执行方式**：
- 基于事件实时触发评估
- 规则命中后产生「告警事件」进入事件流
- 规则版本化管理，变更可追溯

**规则结构（示意）**：
```
规则: 登机保障人员不足
  条件: 航班.登机阶段 AND 登机保障.在岗人数 < 登机保障.需求人数
  动作: 产生「人员资源事件」(级别=告警)
  通知: 登机口班组长、值班经理
  阈值(可配): 需求人数=2, 预警阈值=1
```

### 6.5 航班数据管理（人工录入先行）

**本期**（假设 A9）：人工录入航班计划与关键动态。

**录入内容**：
- 航班计划：航班号、机型、起降机场、计划起降时间、机位、登机口
- 航班动态：实际起降时间、延误、登机口变更、取消

**对接预留**：
- 航班数据接收 API：`POST /api/v1/flights/sync`
- 后续对接 FOC/DCAC 时，由对接层写入，替换人工录入
- 数据模型设计兼容外部系统字段（如航班任务号、机尾号）

---

## 7. 用户旅程

### 7.1 值班经理

```
登录 → 查看运行态势总览
     → 发现风险航班（红色标记）
     → 下钻查看航班保障链路
     → 查看保障能力是否充足
     → 查看事件处理进度
     → 介入处置重大事件
     → 班后查看分析中心复盘
```

### 7.2 班组长

```
登录 → 进入岗位工作台
     → 查看本班组负责航班
     → 查看岗位资源（人员状态）
     → 接收异常事件通知
     → 分配/调度人员
     → 跟踪任务完成
     → 闭环保障任务
```

### 7.3 一线保障人员

```
登录（移动端）→ 系统主动推送本人相关任务
              → 查看任务详情（航班、岗位、时间、地点）
              → 接收任务（状态→保障中）
              → 执行保障工作
              → 完成任务上报（状态→已完成→空闲）
              → 异常情况上报事件
```

---

## 8. 功能模块详细设计

### 8.1 运行态势中心（值班经理视角）

- **整体看板**：今日航班数、在保障航班数、风险航班数、预警/告警事件数
- **航班列表**：按风险等级排序，支持按状态/机位/登机口筛选
- **保障能力热力图**：各岗位资源满足情况一览
- **实时刷新**：30 秒轮询 + 关键事件 WebSocket 推送

### 8.2 航班详情

- 航班基本信息（计划/实际时间、机位、登机口、机型）
- 保障任务链路：按阶段展示任务及状态（待开始/进行中/已完成/超时）
- 参与人员清单及实时状态
- 关联事件列表
- 保障能力实时计算结果

### 8.3 事件中心

- 事件列表（按级别/状态/类型/时间筛选）
- 事件详情：影响范围、处理记录、时间线
- 事件处理：认领、处理、关闭、复盘
- 人工上报入口（移动端 + Web）

### 8.4 岗位工作台（班组长）

- 本班组负责航班概览
- 岗位人员状态看板（空闲/保障中/不可用/未知）
- 任务分配与调度
- 异常处理入口

### 8.5 人员状态（管理视角）

- 全体/班组人员状态总览
- 单人状态详情（当前任务、历史状态）
- 状态异常预警（未知状态人员）
- 预留：设备感知数据展示（本期为空）

### 8.6 任务库（系统管理员）

- 阶段/任务模板 CRUD
- 需求岗位、需求人数、超时阈值配置
- 任务模板版本管理

### 8.7 规则引擎（系统管理员）

- 规则列表（按类型）
- 阈值参数配置
- 规则启用/停用
- 规则命中历史查询

### 8.8 运行数据分析中心（假设 A11）

**定位**：分析航空保障过程中的**历史运行数据**，帮助管理人员了解运行效率、发现问题原因、优化保障资源配置。**不是实时运行监控模块，不替代事件中心和运行态势中心。**

**当前阶段不实现** AI 预测、机器学习模型或自动预测功能；仅在系统架构和接口层预留 AI 预测能力。

**权限**：管理员（查看全部）/ 值班经理（查看运行相关数据）/ 班组长（查看负责区域和岗位数据）；**普通保障人员不开放**。

**模块独立性**：使用已有业务数据计算，不影响现有实时保障功能。

#### 8.8.1 保障运行分析

**目标**：分析航班保障整体运行情况。

**统计指标**：
- 保障航班数量
- 正常完成航班数量
- 异常航班数量
- 事件发生数量
- 平均事件处理时间
- 平均任务完成时间

**时间维度**：日统计 / 周统计 / 月统计 / 自定义时间范围查询

**展示形式**：数据卡片、折线图、柱状图、趋势图

#### 8.8.2 事件分析

**目标**：分析保障异常来源和处理效率。

**事件类型分布**：
- 登机口变化事件
- 人员资源不足事件
- 任务未启动事件
- 特殊旅客保障事件
- 设备异常事件

**分析维度**：
1. 事件数量排名
2. 不同事件平均处理时间
3. 不同事件影响航班数量
4. 事件关闭率

**筛选条件**：时间、航班、事件类型、风险等级

#### 8.8.3 资源分析

**目标**：分析保障资源利用情况。**不是考勤分析**，不统计上班时间、打卡次数、员工工作时长排名，**只分析保障资源能力**。

**分析对象**：岗位、人员、保障任务

**人员状态统计指标**：
- 空闲次数
- 保障中次数
- 完成任务次数

**岗位分析指标**（示例）：

| 岗位 | 任务数量 | 风险次数 | 资源不足次数 |
|------|----------|----------|--------------|
| 登机口岗位 | … | … | … |

**输出目标**：帮助管理人员判断
- 哪些岗位压力最大
- 哪些时间段资源不足
- 哪些岗位需要优化配置

#### 8.8.4 趋势分析

**目标**：发现运行变化趋势。

**时间维度**：日 / 周 / 月

**分析示例**（近 30 天）：
- 异常事件趋势
- 航班保障压力趋势
- 岗位资源不足趋势

**展示形式**：折线图

#### 8.8.5 AI 预测接口（仅预留，不实现）

**当前阶段**：只建立 prediction API 接口层与数据结构，**不生成任何预测结果**。

**未来支持（不本期实现）**：
- 输入：历史保障数据、航班数据、人员状态数据、事件数据、任务完成数据
- 输出：航班保障风险预测、人员资源需求预测、事件发生概率预测

**预留接口示例**：

```
POST /api/prediction/analyze

请求:
{
  "flightId": "",
  "historicalData": "",
  "operationData": ""
}

响应（本期固定返回，表示模块预留）:
{
  "predictionStatus": "not_enabled",
  "message": "AI prediction module reserved"
}
```

> 本期该接口仅返回「未启用」状态，不调用任何模型，不返回预测结果。

### 8.9 移动端（一线人员）

- 任务推送与列表
- 任务详情与接收/完成
- 异常事件上报
- 个人状态查看
- 消息中心

---

## 9. 数据模型（核心实体）

> 仅列核心实体与关键字段，详细 schema 在实现阶段细化。

### 9.1 实体关系概览

```
航班 (Flight) 1───* 任务实例 (TaskInstance)
                      │
                      │ 需求
                      ▼
                 任务模板 (TaskTemplate) ── 阶段 (Phase)
                      │
岗位类别 (PositionCategory) 1───* 岗位 (Position) 1───* 能力标签 (Capability)
                      │
用户 (User) *───* 岗位 (通过 UserPosition)
                      │
                      │ 执行
任务实例 *───* 用户 (通过 TaskAssignment)
                      │
                      │ 产生
事件 (Event) 1───* 事件处理记录 (EventHandleLog)
                      │
规则 (Rule) ── 触发 ──► 事件
```

### 9.2 核心实体字段（摘要）

**Flight（航班）**
- flight_id, flight_no, aircraft_type, origin, destination
- planned_departure, planned_arrival, actual_departure, actual_arrival
- gate, stand, status, source(manual/sync), created_at, updated_at

**TaskTemplate（任务模板）**
- template_id, name, phase, required_position, required_capability
- required_count, timeout_seconds, warning_advance_seconds, version

**TaskInstance（任务实例）**
- task_id, flight_id, template_id, planned_start, planned_end
- actual_start, actual_end, status, assigned_count

**User（用户）**
- user_id, name, employee_no, phone, role, status, auth_id

**UserPosition（用户岗位）**
- user_id, position_id, is_primary

**PersonnelStatus（人员状态）**
- user_id, status, current_task_id, last_event_time, source(task/rfid)
  - source 本期仅 task；rfid 为后期 RFID 设备接入预留

**Event（事件）**
- event_id, type, level, source(auto/manual), trigger_time
- flight_id, task_id, affected_positions[], affected_users[]
- status, handle_logs[]

**Rule（规则）**
- rule_id, type, name, condition, action, thresholds{}, enabled, version

### 9.3 运行数据分析中心数据表（新增，假设 A11）

> 用于运行数据分析中心的统计与聚合。基于已有业务数据计算，不影响实时保障功能。

**1. flight_operation_statistics（航班运行统计表）**
- flight_id, date, task_count, event_count, completion_rate
- 可扩展：normal_completed_count, abnormal_count, avg_task_duration

**2. event_statistics（事件统计表）**
- event_type, count, avg_process_time, close_rate
- 可扩展：stat_date, affected_flight_count

**3. resource_statistics（资源统计表）**
- role_type, task_count, active_count, risk_count
- 可扩展：stat_date, idle_count, busy_count, completed_count
- 说明：role_type 对应岗位；**不记录考勤字段**（无上班时间/打卡次数/工作时长）

**4. prediction_interface（预测接口表，预留）**
- request_id, request_payload, prediction_status, created_at
- 本期 prediction_status 固定为 "not_enabled"，不存储任何预测结果

---

## 10. 非功能性需求

### 10.1 性能（假设 A5）

- 运行态势看板首屏加载 ≤ 2 秒
- 任务列表查询响应 ≤ 1 秒
- 事件产生到推送送达 ≤ 5 秒（站内）/ ≤ 10 秒（App/IM）
- 运行数据分析中心报表查询响应 ≤ 3 秒（含聚合）
- 峰值并发约 500，支持 2000–3000 一线人员注册（已扩容）

### 10.2 可用性

- 核心业务可用性 ≥ 99.5%（按工作时间 6:00–24:00）
- 单点故障不影响保障任务执行（移动端可离线查看已接收任务）

### 10.3 安全性

- 全链路 HTTPS
- 数据范围权限控制（班组/个人隔离）
- 操作审计日志（关键操作可追溯）
- 核心数据不出内网（混合云架构保障）

### 10.4 数据保留（假设 A7）

- 航班保障过程数据 ≥ 1 年
- 事件数据 ≥ 2 年
- 审计日志 ≥ 1 年
- 分析中心聚合数据 ≥ 3 年

### 10.5 可扩展性

- 航班数据接入层抽象，支持后续对接外部系统
- 设备状态上报接口预留，支持硬件感知接入
- 规则引擎可扩展新规则类型
- 多机场/分公司支持预留（数据隔离字段）

---

## 11. 关键流程

### 11.1 航班保障主流程

```
1. 航班计划录入（人工/后续对接）
2. 系统按任务库生成该航班任务实例
3. 系统计算各任务需求岗位与人数
4. 匹配具备能力的空闲人员，自动/人工分配
5. 推送任务给一线人员（站内+App）
6. 一线人员接收任务，状态→保障中
7. 任务执行，状态实时更新
8. 任务完成，状态→已完成→空闲
9. 全部任务完成，航班保障闭环
10. 过程中任何异常→事件→规则评估→精准推送
```

### 11.2 事件处理流程

```
1. 事件产生（自动规则命中 / 人工上报）
2. 系统进行影响分析（关联航班、岗位、人员）
3. 按精准推送策略通知相关人员
4. 相关人员认领并处理
5. 处理过程记录入事件日志
6. 事件关闭
7. 可选：复盘记录
```

### 11.3 登机口变更场景（示例）

```
触发: 航班 CA1234 登机口由 A05 变更为 A12
  ↓
事件: 产生「航班变化事件」(级别=告警)
  ↓
影响分析: 影响岗位 = 登机口、广播、引导
  ↓
精准推送: 仅通知 CA1234 的登机口/广播/引导人员 + 班组长
  ↓
处理: 相关人员确认变更，更新任务地点
  ↓
关闭: 所有相关人员确认，事件关闭
```

---

## 12. 边缘场景

| 场景 | 处理策略 |
|------|----------|
| 一线人员离线时收到任务推送 | 上线后补推；任务在 App 内可见 |
| 同一人员被分配冲突任务（时间重叠） | 规则引擎「资源冲突规则」命中，产生告警事件，班组长介入调度 |
| 航班临时取消 | 产生航班变化事件，关联任务实例批量置为「已取消」，通知所有已分配人员 |
| 人员长时间无状态更新 | 状态转「未知」，产生人员资源事件预警班组长 |
| 登机口临近变更，人员已到原登机口 | 紧急推送 + 短信保底，记录处理时长 |
| 规则引擎误报 | 支持事件「误报」标记，关闭不计入考核；规则阈值可调整 |
| 人工录入航班数据与后续对接数据冲突 | 以对接系统数据为准，人工数据标记为「待校验」并提示 |
| 设备感知数据与任务事件状态冲突 | 本期无 RFID 设备数据；后期按优先级融合，RFID 数据为补充输入源 |
| 高峰期推送洪峰 | 事件合并 + 推送队列削峰；同一用户短时去重 |

---

## 13. 验收标准

### 13.1 功能验收

- [ ] 系统管理员可维护岗位/任务库/规则/用户
- [ ] 录入航班计划后，系统自动生成该航班全部任务实例
- [ ] 任务可分配给具备能力的人员并推送
- [ ] 一线人员可在移动端接收、执行、完成任务，状态实时更新
- [ ] 人员状态按任务事件正确流转（空闲/保障中/已完成/不可用/未知）
- [ ] 保障能力可实时计算并展示（正常/预警/不足）
- [ ] 航班变化/任务超时/人员不足等可自动产生事件
- [ ] 事件按影响范围精准推送给相关岗位人员，无关人员不收到
- [ ] 推送支持站内 + App + 企业 IM 至少各一种
- [ ] 规则阈值可由管理员配置并生效
- [ ] 值班经理可查看运行态势、风险航班、保障能力、事件处理
- [ ] 班组长可在工作台管理本班组资源并处理异常
- [ ] 运行数据分析中心可展示保障运行分析（航班数/正常完成/异常/事件数/平均处理时间/平均任务完成时间）
- [ ] 运行数据分析中心可展示事件分析（类型分布、数量排名、平均处理时间、影响航班数、关闭率）
- [ ] 运行数据分析中心可展示资源分析（岗位任务数/风险次数/资源不足次数，不含考勤字段）
- [ ] 运行数据分析中心可展示趋势分析（日/周/月折线图）
- [ ] 运行数据分析中心支持日/周/月/自定义时间范围查询
- [ ] 运行数据分析中心权限生效：管理员全部、值班经理运行数据、班组长负责区域、一线人员不开放
- [ ] AI 预测接口 `POST /api/prediction/analyze` 可访问且固定返回 `predictionStatus: not_enabled`
- [ ] 本期系统不生成任何 AI 预测结果

### 13.2 非功能验收

- [ ] 态势看板首屏 ≤ 2 秒，任务查询 ≤ 1 秒
- [ ] 事件产生到站内推送送达 ≤ 5 秒
- [ ] 运行数据分析中心报表查询 ≤ 3 秒
- [ ] 数据范围权限生效（班组长只见本班组，一线只见本人；分析中心对一线不开放）
- [ ] 核心数据不出内网；云端仅授权访问通知/任务摘要
- [ ] 关键操作有审计日志
- [ ] 运行数据分析中心模块独立，不影响现有实时保障功能

---

## 14. 后续演进预留

| 演进方向 | 本期预留 |
|----------|----------|
| RFID 等硬件感知接入 | 设备状态上报 API、双源状态融合策略、PersonnelStatus.source 字段 |
| FOC/DCAD 对接 | 航班数据接收 API、数据模型兼容字段 |
| AI 预测（阶段三） | prediction API 接口层、prediction_interface 表、分析中心数据沉淀 |
| 多机场/分公司 | 数据隔离字段、可配置岗位体系 |
| 短信通道 | 推送通道抽象，可插拔 |

---

## 15. 评审清单

评审时请重点确认以下「关键假设」（§0）是否成立，以及：

1. 阶段一+二范围 + 运行数据分析中心是否准确；阶段三 AI 预测仅预留接口是否可接受
2. 混合云部署边界是否清晰（敏感数据内网、员工通知云端授权访问）
3. 人员状态软件先行、RFID 等硬件后期接入的路径是否认可
4. 精准推送通道组合是否满足现场需求
5. 规则引擎「预置 + 阈值可配」是否满足灵活性要求
6. 用户规模假设（2000–3000 一线 / 100–200 管理 / 500 并发）是否符合实际
7. 非功能性指标（性能/可用性/数据保留）是否达标
8. 运行数据分析中心权限（管理员/值班经理/班组长，一线不开放）是否认可
9. AI 预测接口预留方式（固定返回 not_enabled）是否满足未来扩展需要

---

*本文档为设计草案，评审通过后将作为 `tech-stack.md` 与 `implementation-plan.md` 的输入依据。*
